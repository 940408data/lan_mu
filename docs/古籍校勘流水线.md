# 古籍校勘流水线（流程与产物总览）

> 本文是 AI 古籍校勘子系统（`collation/`）的**全流程总览**：从最初的输入开始，每一步做什么、产物是什么、谁消费。
> §5 重构方案（①②③④⑥）已批准并落地（dev 1d1ea88）；⑤ 中庸重跑进行中。
> 配套：`collation/DESIGN.md`（设计原点）、`docs/古籍校勘实施实录.md`（大学实施过程与方法论）、`docs/古籍校勘推广指南.md`（推广操作）、`docs/人工精校台设计.md`（P7 精校台与两方案对比）、`.claude/skills/guji-jiaokan/SKILL.md`（可复用技能）。

## 0. 一图总览

```
起始输入（§1）
  │
  ▼
P0 装载 io.js ──────────────→ 内存 Edition 对象（善本/现代本页行）
  │
  ▼
M0 版面抽样 layout-probe.js ─→ layout.json（列×行/格字制/顶退格规则/抽样页，落盘可复审）
  │
  ▼
M2 互证干净底本（仅善本侧）
  recollate.js ─────────────→ recollate-<起>-<止>.json（视觉重OCR+逐字互证）
  build-v2.js ──────────────→ shanben-v2.json + pending-verify.json
  verify-v2.js ─────────────→ 直改回填 shanben-v2.json（终态唯一）+ verify-report.json
  │
  ▼
M3 版面判定经注（版面结构先行，读 layout.json 网格）
  judge-grid.js ────────────→ grid.json（页×列×{顶/退格,文字}）
  verify-jz.js ─────────────→ 判定准确率（借既有 works 作 ground truth，只核验不依赖）
  build-songke.js ──────────→ output/善本点校本-分栏.md（经注分栏）
  │
  ▼
P3 对齐 align.js ───────────→ aligned.json（段级对齐，剥书题/校记，异体归一仅用于匹配）
  ▼
P4 对校 diff.js ────────────→ diffs.json（字级：异体/真异文/ocr疑）+ clusters.json（簇级夺/衍/换原生归并）
  ▼
P4.5 簇核验 cluster.js ─────→ clusters-verify.json（规则归类 + 双侧视觉四分类）
  │    善本底本误 → apply-basefix.js 回修 shanben-v2 → 重跑 P3 起（结论按内容键迁移，收敛即止）
  ▼
P5 校书官 officer.js ───────→ verdicts.json（真异文+ocr疑+核验为真的簇；证据分级加权+β+悬置三规则）
  ▼
P6 出具 export.js ──────────→ output/{善本点校本,现代本,校勘记}.md + 精校台.html + flags.yaml
  ▼
P7 人工精校台（V键书影/键盘流）→ decisions.json → --step=apply 回灌 → 重出定本
  │
  ▼
M6 进引擎 build-works.js ───→ works/<新id>/ 四件套 → npm run build 验证
```

## 1. 起始输入（一切开始之前）

| 输入 | 路径 | 说明 |
|---|---|---|
| 善本扫描 | `input_data/<书>/当涂郡本_pdf/page_XXXX.pdf` | 公开善本分页 PDF（gitignored，运行期读取） |
| 善本旧 OCR | `input_data/<书>/当涂郡本_ocr/page_XXXX.md` | 外部 OCR 产物，每页一 md |
| 现代本扫描 | `input_data/<书>/儒藏本_pdf/page_XXXX.pdf` | 现代点校本分页 PDF |
| 现代本 OCR | `input_data/<书>/儒藏本_ocr/page_XXXX.md` | 同上 |
| 版本登记 | `collation/config/editions.yaml` | editions（id/role/level/ocrDir/pdfDir）+ works（书名→两版本映射） |
| 视觉配置 | `collation/config/vision.yaml` | 初校(qwen3.7-plus)/覆校(qwen3.8-max)、阈值 0.7、思考开关、dpi、keyEnv |
| 校书官画像 | `collation/officers/{liu-xiang,jie-xian,dai-zhen,ji-yun}.md` | 四官 system prompt，Node LLM 与 Claude Agent 共用 |
| 既有兰木作品 | `works/<id>/`（可选） | 两个用途：verify-jz 的 ground truth 核验基准；build-works 的 meta 模板 |
| 百炼 key | env `DASHSCOPE_API_KEY` | 逗号分隔多 key 轮询；脚本 resolveApiKey 自读 env 或 ~/.bashrc，**不入仓、不嵌命令行** |

**数据落盘约定**：一切中间产物写 `collation/data/<书>/`，最终文本写 `collation/data/<书>/output/`（经 `docs/collation-data` 软链挂 docsify）。

## 2. 逐阶段详解

### P0 装载 — `src/io.js`
- **做什么**：读 editions.yaml + 两本 OCR 页 → 内存 Edition 对象 `{id, role, level, pages:[{n, lines, raw, isCover}]}`；登记 PDF 页数。
- **产物**：无落盘（内存）。封面页自动标记 isCover。

### M0 版面抽样 — `tools/layout-probe.js`
- **做什么**：视觉抽样 3–5 页善本（默认前/中/后），只总结**版面物理结构**：几列几行、每格一字还是双行夹注、顶格/退格规则、版心鱼尾书题位置。**每书必做，每书不同**。
- **产物**：`data/<书>/layout.json`（落盘：网格/规则/抽样页/probe 原文），judge-grid 默认读它（--cols/--rows 可覆盖）。
- **质量闸**：抽样结论须能解释正文的全部列起始形态。实测：大学、中庸同为当涂本 16列×15行、每格一字、顶格经/退格注。

### M2 互证干净底本（三工具一循环，仅善本侧）
- **M2a `tools/recollate.js`**：渲染善本页 → 视觉重 OCR（关思考，快 5.7×）→ 与旧 OCR 逐字编辑距离比对 → `recollate-<起>-<止>.json`（每页 agree 一致率、diffs、visText）。**闸：平均一致率 ≥90%**，过低查版面/OCR 质量。大学实测 92.9%。
- **M2b `tools/build-v2.js`**：两路对齐后规则仲裁（一致采信 / 视觉缺字采旧 / 视觉多字舍 / 异体存善本原刻字形 / 真异→暂采旧+列待覆校）→ `shanben-v2.json` + `pending-verify.json`。**对齐用归一字、输出用原刻字形**——善本古异体不丢。
- **M2c `tools/verify-v2.js`**：pending 按页分组渲染扫描，verifyChars 批量问「实印何字」（初校开思考，conf<0.7 升覆校）→ **直改回填 `shanben-v2.json`（终态唯一）** + `verify-report.json` 留痕。大学纠旧 OCR 误读 40 处；中庸 68 处疑难改字 51、维持 17。

### M3 版面判定经注（版面结构先行）
- **M3a `tools/judge-grid.js`**：逐页 gridColumns（列级：起始顶格/退格 + 列文字）→ 规则判经注（顶格=经 j、退格=注 z）→ `grid.json`。断点续传、并行、开思考。
- **M3b `tools/verify-jz.js`**：以既有 works 的 text.yaml 为 ground truth 核验判定准确率（大学 91.7%）。**只核验、不反哺**（否则失去通用性）。
- **M3c `tools/build-songke.js`**：grid.json → 经注分栏 `output/善本点校本-分栏.md`（大学 129–134 blocks）。

### P3 对齐 — `src/align.js`
- **做什么**：善本逐字 token（带页/列溯源；优先 shanben-v2.json）× 现代本句子，句锚点 + 编辑距离对齐。异体表 VARIANT_MAP **仅用于匹配归一，不改正文**；剥页中书题/内联校记；朱子章句强锚（右傳之/右經一章/右第X章）。
- **产物**：`aligned.json`（段级：segId/score/orphan/现代句/现代页/善本串）。`--step=align` 单跑。

### P4 对校 — `src/diff.js`
- **做什么**：对齐段平坦化为两级异文。**字级**（v 编号）：异体（归一合、形异，登记不裁）/ 真异文（两本各通）/ ocr疑。**簇级**（c 编号，原生归并）：同段内相邻（间隔 ≤2 同字）夺/衍合成短语级簇——夺簇/衍簇/换簇（措辞异）/ orphan 整段。
- **产物**：`diffs.json` + `clusters.json`。大学：字级 105（真异文 36 / 异体 69）+ 簇 33；中庸：字级 311（真异文 68 / 异体 243）+ 簇 72。

### P4.5 簇核验 — `src/cluster.js`（verify 步骤）
- **做什么**：逐簇定谳。**规则类**不动视觉：书题/页眉残字、牌记题跋页（按书登记起页）、现代本自带校记/音注脚注（原脱/未詳/去聲…）。**其余双侧视觉核验**：善本页+现代本页同呈一图call，按锚点定位、如实照录两侧实印，四分类——现代OCR误 / 善本底本误 / 真异文 / 书题牌记。初校→覆校路由，增量保存断点续传。
- **回路**：善本底本误 → `tools/apply-basefix.js` 回修 shanben-v2（全锚定位；删除可短语回退；orphan 用核验录得的「善本前邻 6 字」定位 + **近邻守卫**防短句误补）→ 重跑 P3 起，核验结论按内容键自动迁移，收敛即止。大学实证：回修 10 处、簇 47→33。
- **产物**：`clusters-verify.json` + `basefix-log.json`（永久修复留档）。

### P5 校书官 — `src/officer.js`
- **做什么**：对**真异文 + ocr疑 + 核验为真的簇**逐条：四官并行各陈意见（带证据等级 grade）→ 加权聚合。**证据四级**（確證1.0/旁證0.7/推測0.4/無證0.15）×置信 = 得分；**底本优先** shanben×1.15；**悬置三规则**：首选占比<0.55 / 首选次选差<0.18 / 弃善本从他本而无確證旁證——居其一即悬置。旧裁决按内容键迁移不重跑。
- **产物**：`verdicts.json`。大学 43 条：resolved 26 / suspended 17（含 7 簇）。

### P6/P7 出具与人工 — `src/export.js` + `src/review.js`
- **产物**：`output/善本点校本.md`（公开，A 级）、`output/现代本.md`（自用，B 级，不入 dist）、`output/校勘记.md`（字级真异文 + 簇级夺衍换 + 异体 + 底本修复留档 + 噪声附录，**每条带 发现/核验/裁决 三段出处** + 四库体例定论句）、`output/精校台.html`（单文件离线）、`flags.yaml`（纯文本兜底）、中间 JSON。
- **P7 精校台**：默认只列悬置条（已决折叠抽查）；键盘流 J/K 移动、1 采善本/2 采现代本/3 两存、**V 展开双侧书影**（低清页图内嵌，裁断看原刻）、E 导出 decisions.json → `run.js <书> --step=apply --decisions=<路径>` 回灌（verdict:human，机器意见原文保留可推翻）→ 自动重出定本。

### M6 进引擎 — `tools/build-works.js`
- **做什么**：grid.json → `works/<新id>/` 四件套（text.yaml 经注分栏 + meta 借 --base 模板 + seals/ornaments）。善本底独立新作品，不动通行本。
- **验证**：`npm run build -- --work=<新id> --only=html`。大学产物 `works/daxue-songben` 已过。

## 3. 产物总表

| 产物 | 生产者 | 消费者 | 重建时机 |
|---|---|---|---|
| `recollate-*.json` | recollate.js | build-v2.js | 底本来源变化 |
| `shanben-v2.json` ★ | build-v2.js / verify-v2.js（直改终态） | **align.js**、build-works 链路 | 互证/覆校/底本回修后 |
| `pending-verify.json` | build-v2.js | verify-v2.js | 随 build-v2 |
| `verify-report.json` | verify-v2.js | 留痕（build-v2 重跑幂等应用） | 覆校后 |
| `layout.json` | layout-probe.js（M0） | judge-grid.js 读参、复审 | M0 抽样后 |
| `grid.json` | judge-grid.js | verify-jz / build-songke / build-works | 版面参数或底本变 |
| `aligned.json` | run.js align/export | diff.js（内存）、排查 | 底本/对齐规则变 |
| `diffs.json` | run.js diff/export | officer、校勘记 | 对齐变 |
| `clusters.json` | run.js diff（diff.js 原生） | run.js verify（cluster.js） | 对校变 |
| `clusters-verify.json` | run.js verify | officer（真簇入裁）、export | 核验后；迁移自动 |
| `basefix-log.json` | apply-basefix.js | export（校勘记第四节） | 底本回修后，永久 |
| `verdicts.json` | officer.js | export.js、apply 回灌 | 裁决更新 |
| `output/善本点校本.md` | export.js | 公开 | 任何上游变 |
| `output/现代本.md` | export.js | 自用（**不入 dist**） | 同上 |
| `output/校勘记.md` | export.js | 审读 | 同上 |
| `output/精校台.html` | export.js（review.js） | 人工 P7 | 同上 |
| `flags.yaml` | export.js | 人工 P7（纯文本兜底） | 同上 |
| `works/<新id>/` | build-works.js | 兰木引擎 | grid.json 定稿后 |

## 4. 两书现状对照

| 阶段 | 大学章句 | 中庸章句 |
|---|---|---|
| M0 版面抽样 | ✅ 16×15、顶格经/退格注（layout.json） | ✅ 16×15；**直读书影发现系晋府刻：经注同顶格同大字，顶退格规则不适用** |
| M2 干净底本 | ✅ 40 页互证 92.9% + 覆校纠 40 处 + 簇回路再修 10 处 | ✅ 78 页互证 98.4% + 覆校 68 处（改 51/维持 17）+ 簇回路修 6 处 |
| M3 版面判定 | ✅ grid.json，核验 91.7%（顶退格规则） | ✅ 投影判定（project-jz，works 语料 LCS），p14/p50 直读书影抽验全中；序页全经 |
| P3/P4 对齐对校 | ✅ 字级 105（真异文 36/异体 69）+ 簇 33 | ✅ 字级 313（真异文 68/异体 245）+ 簇 67 |
| P4.5 簇核验 | ✅ 收敛（真 7/噪声 25/底本已修 12 待修 1） | ✅ 收敛（真 30/噪声 37/底本已修 6） |
| P5 校书官 | ✅ 43 条（resolved 26/suspended 17，含 7 簇） | ✅ 98 条（resolved 86/suspended 12，含 30 簇） |
| P6/P7 出具+精校台 | ✅ 校勘记新体例 + 精校台.html | ✅ 同（悬置 12，书影 22 页内嵌） |
| M6 进引擎 | ✅ works/daxue-songben（expect 回填 5957/1944/4013） | ✅ works/zhongyong-songben（34 节/正文 334 blocks，expect 13396/4346/9050） |

## 5. 缺口处置状态（①–⑥ 全部落地，两书走通）

- **① P4.5 正式化 ✅**（dev 1d1ea88）：diff 原生簇级、verify 正式环节、officer 扩围、export 环节出处汇总、底本回修回路、双迁移机制。
- **② M0 落盘 ✅**：layout-probe.js → layout.json，judge-grid 读参。
- **③ 底本终态唯一 ✅**：verify-v2 直改 shanben-v2.json。
- **④ run.js 全链 ✅**：`--step=all` = align→diff→verify→officer→export；`--step=apply` 回灌。
- **⑤ 中庸重跑 ✅**（dev ffde8d7 + df77ddc）：全阶段走通进引擎。**新经验**：同丛书版式未必同（中庸系晋府刻，经注同顶格同大字）——verify-jz 准确率（56.8%）是报警器；版面无区分时以 project-jz 投影判定 + 直读书影抽验。
- **⑥ 精校台+裁决升级 ✅**：review.js 精校台（V 键书影）+ 证据分级加权 + β=0.15 + 悬置三规则 + 四官限权约束。
- **后续候选（另批）**：异文分类细化（通假/避讳/倒文）、RuleSegmenter 独立句读+Jaccard 版权闸、bbox 级书影定位、契约测试、中庸序 zhongyongxu-songben 成卷、论语/孟子大部头推广——见 `docs/人工精校台设计.md` §2 与 `docs/古籍校勘推广指南.md`。

## 6. 质量闸汇总

| 环节 | 闸 | 标准 |
|---|---|---|
| M0 | 版面结论可解释全部列形态 | 抽样 ≥3 页一致 |
| M2a | 两路一致率 | 平均 ≥90% |
| M2c | 覆校置信 | conf ≥0.7，不足升覆校 |
| M3b | 经注判定准确率 | ≥90%（有 ground truth 时） |
| P4.5 | 簇核验覆盖+收敛 | 全部簇有结论；底本误回修后重跑至无新增 |
| P5 | 校书官 | 4 官齐全、无 error |
| M6 | 引擎构建 | `build --only=html` 通过 |
