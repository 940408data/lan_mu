# 《中庸章句》新底本重跑复盘

> 工作分支：`engine/collation-cleanup-first`。本轮不覆盖远端 `dev`；`dev-zhongyong-archive` 保留原 `dev`，最终以审阅结果决定是否更新 `dev-cleanup`。

## 本轮已完成

### M2：清洗、互证、覆校

- 输入：`recollate-1-78.json`，当前 `shanben-v2.json` SHA-256：`0ad78d73ed448fb9ec4b31217af41ef9289b7bf85d1823dd384cf7e226864ab4`。
- 78 页、14,463 字；两路 OCR 仲裁为一致 14,317、视觉缺字采旧 78、视觉多字舍 52、覆校实字 68；`pending-verify.json` 已清零。
- 视觉报告逐项以页号、位置、旧字、现代字、上下文校验后导入；没有复制旧 `shanben-v2`。这一步是由于执行器对子进程/大图网络请求不稳定，导入同一 `recollate` 输入上的已完成视觉意见，随后由当前分支 `build-v2.js` 重新生成 M2。

### M3：版面与经注

- 重新对当前 M2 逐页判定 2–76 页（75 页），网格 SHA 与 M2 一致：1,224 列，其中经 913、注 311。
- `layout.json` 固定正文页域为 7–76；2–6 为序，77 为修版/音注页，78 为空白封底。
- M6 章节分节已迁移中庸经验：`build-works.js` 识别章末 `右第X章`，生成首章加第三十三章共 34 sections；同时保留 M2/grid hash 与 pending 质量闸。
- 新增 `project-jz.js` 作为“经注同顶格”刻本的备援投影判定工具；本轮当涂本有明确顶格/退格规则，未使用投影覆盖视觉结果。

### P3–P6、P5-b 与句读

- P3/P4：1,157 对齐段；字级异文 303（真异文 61、异体 242），簇 50（夺 27、衍 18、换 5）。
- P5：61 条真异文完成校书官裁决；50 个簇已全部完成 P4.5 双侧核验（书题牌记 13、真异文 23、现代本 OCR 误 9、善本底本误 5）。5 条“善本底本误”只登记为待回修，尚未自动改写 M2；因此不能把当前公开成果视为终稿。
- P5-b 全篇审查：26 个模型分块、181 条模型建议、model score 78、fallback 0；与确定性扫描合并后质量状态仍为 `blocked`，主要阻断是 4 处相邻善本文字重叠，另有 8 个 orphan 和 5 个低分对齐段。
- 模型句读：1,149 条建议，972 条通过阈值并应用，177 条保留 pending；字符骨架校验通过，标点来源标为 `llm-approved`，不改动任何汉字。

### M6 与构建验证

- `works/zhongyong-songben/` 已按正文页域和 34 章生成；Unicode 扩展字按 code point 计数，句读/括号不占字格，`npm run validate` 已通过。
- `npm run collate:test` 已通过；`daxue-songben` 与 `zhongyong-songben` HTML 均已构建成功（字体缺失时使用系统回退链）。

## 结论

这轮已经把“新 M2 → 新 M3 → 新对齐/对校 → P5-b → 句读 → M6”串起来，且所有公开中间产物都带当前 M2 证据链。文本质量没有因清洗自动变成“可发布”：中庸的主要风险已从 OCR 单字层转移到跨段对齐/重复消费和现代本孤段，必须按质量闸处理。

## 下一步

1. 对 5 条“善本底本误”逐条回到书影，确认是底本漏录/错录；只有确认后才运行 `apply-basefix`。一旦回修，必须从 M3 起重跑全链，不能只改公开文本。
2. 对 4 处相邻重叠逐处回到善本页和现代本页，确认是跨页重复、现代本残留还是对齐边界错误；必要时修正对齐而不是直接改正文。
3. 对 8 个 orphan、5 个低分段进行人工/模型定位；在结构性阻断解除前，P5-b 结论只能作为审查报告，不能授权模型直接删改正文。
4. 句读 pending 177 条只作为审阅队列；未通过字符骨架或置信阈值的建议不得自动应用。
5. 当前成果保持 `draft/blocked`，先供审阅；审阅通过后再更新 `dev-cleanup` 并 push，最后才考虑合并远端 `dev`。
